{{ form_start(form) }}
<div class="form-group">
    {{form_row(form.content)}}
    {{ form_rest(form) }}
    <button class="btn btn-primary  btn-lg btn-block">{{ button_label|default('Envoyer la question !') }}</button>
</div>
{{ form_end(form) }}
